"use client";

import { Segmented as AntdSegmented, Spin as AntdSpin, Table as AntdTable, Tag as AntdTag } from "antd";

/** 标签：只在需要语义色文字 + 1px 描边时使用。 */
export const Tag = AntdTag;

/** 表格：仅设置页使用。 */
export const Table = AntdTable;

/** 分段控件：视图切换。 */
export const Segmented = AntdSegmented;

/** 加载指示。 */
export const Spin = AntdSpin;
